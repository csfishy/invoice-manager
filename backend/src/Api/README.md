# Api Layer

Place the ASP.NET Core Web API host in this folder.

Expected responsibilities:

- controllers or minimal API endpoint registration
- authentication and authorization setup
- DTO validation wiring
- dependency injection composition
- startup checks for license and required configuration
